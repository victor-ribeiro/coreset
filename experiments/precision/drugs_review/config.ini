[experiment]
name = drug_reviews
task = classification

[experiment.config]
output = precision_drug_reviews.csv

[dataset.info]
file_name = drugsComTrain_raw.tsv
columns = ['drugName', 'condition', 'review', 'rating', 'date', 'usefulCount']
target = usefulCount
